name: flutter_architecture_e1
description: "Flutter Architecture Template E1 = Semi Clean Architecture. Modular Presentation Approach."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  http: ^1.2.1
  fpdart: ^1.1.0
  get_it: ^7.6.0
  connectivity_plus: ^6.0.1
  equatable: ^2.0.7
  gap: ^3.0.1
  extended_image: ^10.0.1
  intl: ^0.20.2
  flutter_rating_bar: ^4.0.1
  go_router: ^15.1.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/failures/
